{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mcuadros/paseo-director/engine/domain/agentoutcome/schemas/needs_validation.schema.json",
  "title": "NeedsValidationAgentOutcomeClaim",
  "type": "object",
  "additionalProperties": false,
  "required": ["outcome", "candidateSha", "baseSha", "checkIds"],
  "properties": {
    "outcome": { "const": "needs_validation" },
    "candidateSha": { "type": "string", "pattern": "^(?:[0-9a-f]{40}|[0-9a-f]{64})$" },
    "baseSha": { "type": "string", "pattern": "^(?:[0-9a-f]{40}|[0-9a-f]{64})$" },
    "checkIds": {
      "type": "array",
      "minItems": 1,
      "maxItems": 128,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1, "maxLength": 128 }
    }
  }
}
