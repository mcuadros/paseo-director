// SPDX-License-Identifier: Apache-2.0

// Package adapters contains infrastructure implementations of engine-owned
// ports. An adapter observes facts or performs one authorized effect; it never
// decides lifecycle state, retry, routing, reconciliation, or closure.
package adapters
