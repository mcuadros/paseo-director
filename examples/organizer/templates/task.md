# {{ key }} — {{ title }}

## Objective

State the bounded outcome and the single target Workspace.

## Prerequisites

List Task or Epic dependencies. Use `None` when the Task is independent.

## Priority and labels

- Priority: `normal`
- Labels: `example`

## Acceptance criteria

- The requested behavior is observable from a supported public surface.
- Focused tests cover success, refusal, and recovery.
- No credential, private path, raw external output, or unrelated change is
  committed.

## Policy notes

State only explicit Task overrides. Every omitted value inherits from its
Workspace and Project.
